[project]
name = "space-invaders"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame",
]

[tool.poetry.scripts]
space-invaders = "space_invaders.__main__:main"