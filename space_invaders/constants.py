"""Class to manage constant values to declutter and simplify the other modules."""

class SIConstants:

    def __init__(self):
        """Initialize constant values."""
        self.SCREEN_WIDTH = 1200
        self.SCREEN_HEIGHT = 800
        self.DARK_PURPLE = (40, 0, 80) # Background color